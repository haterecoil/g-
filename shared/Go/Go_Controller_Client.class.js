var Go_Controller_Client = function () {
    // inherit parent Go_Controller  
};

Go_Controller_Client.placerPierre = function(x,y) {
    // appeler placerPierre() du parent
    // envoyer au serveur (via une autre classe ?) l'action qu'on vient de faire
}